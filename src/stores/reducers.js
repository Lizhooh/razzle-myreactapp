
export { default as index } from './actions/index';

export default (state = {}, action) => {
    return state;
}

export const about = (state = { name: 'aaa' }, action) => {
    return state;
}
